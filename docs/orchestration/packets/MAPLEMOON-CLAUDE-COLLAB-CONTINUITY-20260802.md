# MapleMoon Claude collaboration continuity

Updated: 2026-08-02 12:53 AEST

## Binding and authority

- Durable coordinator: Codex Main Boss `019fa858-05c9-7631-b26e-8f5cbbf1387a`.
- Disposable console that created this record: Side conversation `019fc04b-9525-7002-814a-f19095ada4d7`.
- Project-local packets, receipts, checkpoints and manifests remain the source of truth.
- Claude is a bounded independent reviewer or one-packet worker, never a second coordinator.
- Main alone owns locks, packet admission, worker routing, acceptance and promotion.
- Nate alone owns literal Chrome 200%/keyboard/ordinary-viewer acceptance and every external action.

## Completed in this Side conversation

- Main was consulted read-only and confirmed the authority model above.
- Nate pasted a read-only Claude intake prompt requesting one compact HANDSHAKE card.
- That prompt requires Claude to verify current local evidence, identify conflicts, propose one bounded packet and stop for Main.
- A short-lived Codex monitor was attempted, but this Side conversation cannot read the embedded Claude terminal (`No app terminal session is attached to this thread yet`). The ineffective monitor was deleted.
- No website file, frozen package, worker, client channel, commerce system or production system was touched.

## Current state

- Claude's HANDSHAKE response is **UNKNOWN** until Nate opens the visible new message and supplies its text or screenshot.
- Do not infer a PASS, mutation, worker identity or completion from the screenshot, terminal title or missing direct attachment.
- Current frozen-package authority and CR-0 gates remain with Main; this continuity note does not supersede any accepted packet or receipt.

## Claude acceptance boundary

The HANDSHAKE is acceptable only if it:

1. keeps Main as sole coordinator and durable local evidence as authority;
2. remains read-only during intake;
3. does not claim Nate's literal Chrome 200%, keyboard/focus or ordinary-viewer gate;
4. does not propose a bulk commit, deploy, send, commerce access or overlapping writer;
5. proposes exactly one bounded next packet and stops for Main to admit it.

## Exactly one next action

Nate opens Claude's visible new message and gives its HANDSHAKE text or screenshot to Main or any replacement Side conversation for review.

## Restart commands

For a replacement Codex Side conversation:

`Boss resume — recovery: /Users/handtomouse/maplemoon-website/docs/orchestration/packets/MAPLEMOON-CLAUDE-COLLAB-CONTINUITY-20260802.md`

For Claude, if continuity is needed:

`Claude resume — read /Users/handtomouse/maplemoon-website/docs/orchestration/packets/MAPLEMOON-CLAUDE-COLLAB-CONTINUITY-20260802.md and return the HANDSHAKE card only. Do not mutate or coordinate workers.`
