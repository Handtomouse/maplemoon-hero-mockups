# MapleMoon Overnight Boss Handoff

**Recorded:** 2026-08-02 00:48 AEST  
**Coordinator thread:** `019fa858-05c9-7631-b26e-8f5cbbf1387a` (`Plan MapleMoon model workflow`)  
**Project:** `/Users/handtomouse/maplemoon-website`  
**Branch / HEAD at handoff:** `codex-maplemoon-section-review` / `d65047b`  
**Authority:** This project-local record and the persistent coordinator thread supersede the disposable side-chat discussion as overnight authority.

## Nate approval

Nate approved all six recommended overnight defaults:

1. Bounded local edits are allowed only through exact non-overwriting recovery checkpoints.
2. A blocked page or packet is quarantined while disjoint, already-authorized work continues.
3. Conservative and reversible defaults are allowed; unresolved factual, client, catalogue or external decisions remain blocked and are not invented.
4. No send, commit, push, deploy, publish, upload, client contact, Shopify, WooCommerce or production action.
5. The laptop remains plugged in with the lid open under `caffeinate`; the iMac is read-only QA/failover unless separately admitted.
6. Morning target is the best independently verified six-page local candidate plus a rollback map, changed-file receipt, QA results and a concise blocked-decision list.

## Overnight execution system

- One persistent Boss owns locks, register, source lineage and promotion.
- GSD is used per bounded packet: plan, execute, verify.
- Superpowers are selective: executing plans, systematic debugging and verification before completion. Do not activate the whole suite indiscriminately.
- Use Low/Fast for deterministic hashes, links, occurrence and checker work.
- Use one Medium mutating owner per page or shared file cluster.
- Use High for independent rendered, accessibility, responsive and integration QA.
- Escalate to the strongest model only for a genuine evidence conflict or replan.
- Permit up to three disjoint read-only checks, but never overlapping writers.

## Packet and rollback loop

Every mutation must follow:

1. Confirm current authority, owner, writable paths and base hashes.
2. Create a timestamped, non-overwriting checkpoint for every writable path.
3. Validate the phase-start gate using `docs/orchestration/SIDECHAT_RECEIPT_GATE.md` and `scripts/check-maplemoon-receipt.py`.
4. Mutate only the admitted scope.
5. Build temporarily first.
6. Run deterministic checks and real rendered QA appropriate to the change.
7. Obtain independent verification; the worker cannot accept its own output.
8. Promote only on PASS. On a correctable failure, restore from the checkpoint or quarantine the candidate and retain evidence.

A packet-specific HOLD must not stall disjoint safe work. Global write activity must HOLD on missing recovery, ownership overlap, base-hash drift, failed required verification, destructive action, unsupported factual/client content or an external-action requirement. Read-only work may continue when safe.

## Machine continuity

- `caffeinate -dimsu` was started and verified through `pmset -g assertions`.
- Verified assertions included `PreventSystemSleep`, `PreventUserIdleSystemSleep` and `PreventUserIdleDisplaySleep` from the `caffeinate` process.
- At verification, the Mac was on AC power and charging.
- The lid must remain open; `caffeinate` is not a reliable closed-lid override.

### Resource guard

At 2026-08-02 01:02 AEST the data volume had approximately 36 GiB available and was 92% allocated. The main `_wip/reviews` directory used about 7.0 GiB, checkpoints about 330 MiB, and the isolated Wave 1D generated-candidates directory about 51 MiB.

- Recheck free space before image generation, large screenshot batches, video/render work or broad checkpoint creation.
- If available space falls below 20 GiB, HOLD new large-output work and continue only low-growth tests, audits, planning and receipts.
- Do not delete, compress, move, offload to iMac/Drive or change storage settings automatically. Storage cleanup or transfer requires a separately admitted recovery/custody packet.
- Preserve source imagery, PSDs, checkpoints, receipts and candidate provenance even when a large-output lane is held.

## Monitor

- Automation ID: `maplemoon-overnight-boss-loop`
- Type: heartbeat attached to coordinator thread `019fa858-05c9-7631-b26e-8f5cbbf1387a`
- Cadence: every 20 minutes, 24 runs (approximately eight hours)
- Notifications: failed runs only
- Each heartbeat either advances one proven-safe bounded phase or returns a compact state receipt with exactly one next action.
- The monitor must not restart completed work, wake stale workers, create duplicate coordinators or trust task titles as authority.

## Morning handoff

Nate's command is `Boss check`.

The Boss should return:

- best verified local six-page candidate and exact served paths;
- files changed and files proven untouched;
- checkpoint and rollback map;
- deterministic and rendered QA results;
- active/held/released ownership;
- factual/client/external blockers;
- exactly one recommended next action.

No artifact becomes externally share-ready without Nate's separate explicit approval.

## Queued Wave 1D non-Shop imagery lane

**Source:** Sidechat 1 additive handoff supplied by Nate.  
**Existing worker:** `019fb5b5-de62-7862-a5fe-06bc59395c21` (`Generate Wave 1D imagery candidates`)  
**Status:** queued for coordinator reconciliation and admission; this record does not itself prove current ownership or authorize overlapping writes.

### Coordination and ownership

- Do not create a replacement imagery worker or restart completed Wave 1D research and candidate exploration.
- Main remains the sole ownership, admission and promotion authority.
- Before resuming, Main must read the worker's latest substantive state and reconcile it with the current register, current six-page package, active Shop candidate and Shop Round 2 imagery work.
- If direct worker access is unavailable, use the durable packet/receipt route with exact worker ID `019fb5b5-de62-7862-a5fe-06bc59395c21`.
- Do not interrupt or overlap active Shop work.
- Wave 1D may proceed only on disjoint non-Shop imagery for Homepage, Our Story, Carob Story, Stockists, FAQ and shared editorial/ritual imagery that does not alter Shop products or the active Shop candidate.
- Shop cards, Shop products, product-image replacements, product crops and lower-range product imagery remain reserved for Shop Round 2 until that owner releases them.
- Queue Shop/product findings for later reconciliation; do not overwrite or compete with Shop Round 2.

### Objective and sequence

Continue from the worker's latest evidence, including the original-v5 low-fidelity comparison, until it produces a complete evidence-backed candidate package for admissible non-Shop gaps. Preserve existing candidates and avoid repeated exploration.

1. Reconcile the latest imagery matrix against the current six-page package.
2. Classify each image/section as approved/current, suitable-needs-crop, wrong, visibly unfinished, placeholder, missing, client-asset-required, safe-to-generate or blocked by factual/identity/provenance evidence.
3. Exhaust approved local photography and supplied brand assets before generation.
4. For safe generative gaps, create genuinely distinct directions rather than superficial variants; avoid repeating the prior too-similar candidate problem.
5. Test candidates in isolated local section previews at desktop and mobile sizes only.
6. QA composition, crop safety, focal hierarchy, lighting, colour, grain/realism, brand fit, overlay space, responsive integrity, generative errors and adjacency consistency.
7. Produce labelled contact sheets with baseline, candidates A/B/C only where useful, recommendation, page/section, limitations and approval status.
8. Recommend one direction per section and ask Nate only when a real rendered A/B/C decision materially changes the result.
9. Continue safe disjoint work while any one visual decision waits.
10. Finish with a durable worker receipt and Main Boss handoff.

### Required evidence

The completion handoff must name exact files, source references/provenance, generation prompts, dimensions/crops, contact sheet, desktop/mobile previews, QA results, approved/recommended/blocked classifications, unresolved client/source inputs, deferred Shop findings and exactly one next action.

Every packet and receipt must use worker ID `019fb5b5-de62-7862-a5fe-06bc59395c21`. Create a timestamped non-overwriting recovery checkpoint before any admitted write. Keep all output isolated, versioned and unwired.

### Wave 1D boundaries

- Do not alter canonical WIP, the clean/annotated six-page package or the active Shop candidate.
- Do not fabricate packaging, labels, products, prices, founders, people, testimonials, stockists, certifications, ingredients, claims or client photography.
- Do not present generated packaging or people as real.
- Do not overwrite source imagery or previously approved candidates.
- No commit, push, deploy, publish, upload, client contact, Shopify, WooCommerce or production action.
- The worker cannot approve or promote its own output.

Main may resume the existing worker only after proving its writable paths are disjoint from current Shop ownership. If overlap exists, admit only the non-Shop scope and leave Shop/product imagery queued.

### Wave 1D worker productive-idle rule

Once Main proves disjoint ownership and admits a packet, it may communicate with and keep existing worker `019fb5b5-de62-7862-a5fe-06bc59395c21` moving without waiting for Nate on every routine step.

If that worker finishes its immediate packet or one section becomes blocked, Main should give it the next smallest authorized Wave 1D task from this order:

1. reconcile the current non-Shop imagery matrix and source provenance;
2. inventory approved local assets and crop/responsive opportunities;
3. QA existing isolated candidates for composition, realism, errors and responsive fit;
4. prepare genuinely distinct candidate directions only for already-classified safe generative gaps;
5. build or improve isolated contact sheets and desktop/mobile section previews;
6. update blocked/client-asset/provenance classifications and deferred Shop findings;
7. complete the durable receipt, source manifest and Main handoff.

The worker may continue another disjoint non-Shop section while one visual decision waits. Main should communicate at phase start, completion, correction and promotion gates rather than continuously polling.

This worker does not own global security, Shopify preparation, Shop/product imagery, canonical WIP, the six-page package, promotion or external actions. If all Wave 1D work is genuinely blocked or current, it must return a receipt and release ownership; Main then continues the global productive-idle ladder.

## Productive-idle fallback ladder

If the Boss reaches a point where no admitted implementation or imagery packet is ready, it must not stop merely because one lane is blocked. It should use `/summon` against current local evidence to select the smallest safe next activity from this ordered ladder.

1. **Hash-aware regression verification:** rerun existing deterministic package, cart/no-network, link, asset, forbidden-claim, clean/annotated parity, responsive-overflow and receipt checks only when the relevant artifact hash changed or a current valid result is missing. Do not burn cycles rerunning unchanged evidence.
2. **Rendered local QA:** inspect current candidates at required desktop/mobile widths, keyboard/focus/reduced-motion states and other locally provable gates. Record unsupported human-only checks as HOLD rather than simulating them.
3. **Security readiness:** use `gsd-secure-phase` principles for a read-only audit of secrets, PII, form/network submission, third-party requests, unsafe links, review-only commerce boundaries, dependency exposure, access separation, rollback and external-action gates. It may prepare a local remediation packet but cannot use credentials, change access or touch live systems.
4. **Shopify readiness preparation:** use `gsd-plan-phase` to prepare evidence-backed local packets for Shopify OS2 theme/section mapping, content and asset migration, WooCommerce-to-Shopify catalogue schema/provenance mapping, navigation, product templates, cart/checkout boundaries, redirects/SEO, analytics/consent, accessibility, performance, UAT, rollback and cutover. Preparation must clearly separate known facts, missing client/catalogue inputs and future external approvals.
5. **Gap verification:** use `gsd-verify-work` principles against a named current phase or artifact, then prepare only the smallest bounded correction packet for genuine failures.
6. **Durable handoff improvement:** reconcile registers, locks, hashes, receipts, source lineage, blockers and next packets so the morning Boss check is complete and restartable.

### Idle-ladder routing rules

- Prefer current local evidence and existing plans before research.
- Never use a broad next-list merely to create activity.
- Never re-research, redesign or regenerate unchanged work.
- Use systematic debugging only for a reproduced failure and verification-before-completion for every proposed PASS.
- Continue another disjoint ladder item when one item needs Nate or client evidence.
- All writes remain subject to exact ownership, recovery checkpoint, temporary-build and independent-verification gates.
- No credential access, live Shopify/WooCommerce/Vercel operation, dependency installation, commit, push, deploy, publish, upload, send, client contact or production action.
- Stop only when every safe ladder item has a current valid receipt or when a global safety blocker prevents all remaining local work.
