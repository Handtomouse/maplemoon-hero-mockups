node_modules/
.vercel
_archive/
BRIEF_COPYWRITING.md
assets/moodboard/
*.bak
*.r20_pre_edit
.DS_Store

# Review tools (not for deploy)
triage.html
triage_paper.html
paper_screenshots/
hero_v3.html
outputs/
assets/hero_shots/v9_gen/
assets/hero_shots/v9_source/
assets/hero_shots/moodboard/*.png
.superpowers/
tools/__pycache__/
homepage_backup.pdf

# --- baseline-commit hygiene (added 15 Jul) ---
_wip/checkpoints/
checkpoints/_theme_bak.*/
lewis_round2_contact_*.png

# --- section-review stabilisation (30 Jul): regenerable review/render output ---
checkpoints/
explorations/
_wip/_section_variants/
_wip/reviews/
assets/hero_videos/gen/
docs/client-review/*/staging-v1/**
# Track the irreplaceable markup of the frozen review artifact (~1.1MB).
# Assets stay ignored: all 63 are byte-duplicates of repo assets/ and are reproducible.
!docs/client-review/*/staging-v1/**/
!docs/client-review/*/staging-v1/**/*.html
!docs/client-review/*/staging-v1/**/*.json
!docs/client-review/*/staging-v1/**/*.css
!docs/client-review/*/staging-v1/**/*.js

# --- Wave-1D imagery custody (2 Aug): unapproved exploratory renders stay out,
# their receipts/manifests/boards stay in. Durable copies: locked worktree 27ab
# + iMac replica. Integrity: _wip/evidence/IMAGERY-PROGRAMME-20260802/CANDIDATE-MANIFEST.sha256
docs/client-review/*/generated-candidates/**/*.png

# --- Founder v04 master custody (3 Aug): full-res Photoshop exports stay out of git,
# their sha256 receipts and downscaled review derivatives stay in. Masters are exported
# from the iMac PSD via Photoshop document scripting, never screen capture.
# Integrity: _wip/evidence/IMAGERY-PROGRAMME-20260802/FOUNDER-V04-MASTER.sha256
_wip/masters/

# Regenerable bytecode: tools/ was already covered, scripts/ was not.
scripts/__pycache__/

# Build artefacts. 4.4G of these were swept into a commit on 24 Aug 2026 and
# blocked a push: two PSDs exceeded the 100MB per-file limit.
# NOTE: these rules only stop FUTURE sweeps. Blobs already tracked in c54d115
# are unaffected by ignore rules and must be untracked separately.
_wip/deploy/generated/
out/

# Disposable one-off CDP harness generated during local QA.
mm_5g_cdp_qa.tmp.mjs
