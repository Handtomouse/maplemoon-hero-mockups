# The path to shipping — what actually has to happen
# Written: 2026-08-03 · macbook Claude Code (worker)
# STATUS: PLAN AND ANALYSIS. Records no verdict, passes no gate, changes nothing.
#         CR-0..CR-4 remain Nate's alone. NATE-HOME-001 remains `pending`.

## What "ship" means here

**Send the frozen six-page package to Carli and Dylan and collect their feedback.** That is the
deliverable. The family-and-friends soft launch (SL-0..3) and the whole Shopify build (SH-0..6)
sit behind it and cannot legitimately start until it happens.

Current state: package built, frozen, hash-verified at `clean/MANIFEST.json = d1c66b1d…`,
**never sent**. Named for Saturday 1 August. **There is still no forward send date recorded
anywhere in the repo.**

---

## THE CONSTRAINT THAT SHAPES THE WHOLE SEQUENCE

**A rebuild voids the freeze, and the CR-0 evidence is bound to the frozen hash.**

- keyboard traversal, 5–6 pages at literal 390 CSS px — commit `9ead886`
- 200% zoom pre-screen, 6 pages — commit `fe3c580`

Both are bound to `d1c66b1d…`. Rebuild and they must be re-run.

> **Therefore: take exactly ONE rebuild. Fix nothing until the defect list is complete.**
> Fixing piecemeal means rebuilding twice and re-running the evidence twice.

---

## PHASE 1 — Complete the defect list (Nate, ~30 min) ← **THE ONLY BOTTLENECK**

Everything downstream is blocked on this and nothing else.

1. Walk all six pages on the review board and record pins as you go.
2. Record decisions on existing pins, **including `NATE-HOME-001`** (still `pending`).

**Reserved to Nate.** CR-0..CR-4 and the pin decisions are his. An agent writing one corrupts
every gate that inherits it.

**What is already known, so it need not be re-found** — confirmed in a live browser
(`LIVE-BROWSER-TRUTH-20260803.md`):

| # | defect | scale |
|---|---|---|
| 1 | `#carob` sits 28px left of the 158px grid at desktop (`NATE-HOME-001`) | one declaration |
| 2 | carob-story says *"The four things everyone asks"* over **3** FAQ items | one word |
| 3 | ~36MB of oversized imagery — homepage 21.7MB, carob-story 9.8MB (loaded twice), shop 4.07MB of PNGs | re-export |
| 4 | `.st-skip-finder` visible at 834 (does **not** overlap the paragraph) | one rule |

**Do NOT re-raise** — disproved live: `VIS-001`, `IMG-001`–`IMG-004`, our-story's "3 dead
sub-nav links", stockists' "internal copy visible", homepage audit #1 and #3. All were
capture or static-analysis artifacts.

---

## PHASE 2 — One fix batch (Codex, packeted)

Single packet, single rebuild. Contents:

- everything from Phase 1
- the four confirmed defects above
- **the Moons rename** across all six pages — decided 2026-08-03, closes audit #34 and the
  cross-page naming row (see `ICON-CREATION-BRIEF-20260803.md` §4.1)
- **the caffeine-free icon** + the build-script asset copy (§8b of the same brief)

All of it edits WIP sources under `LOCK_MANIFEST.json`. **Codex is lock custodian; this needs an
admitted packet.** `SAT-HOME-CAROB-HORIZONTAL-BALANCE-01` is still a proposal — no packet file
exists yet.

**Not in this batch:** the new icons (§4 of the brief). They are external design work measured
in days and must not block the send. Carli reacting to the current state is useful input.

---

## PHASE 3 — Re-verify and re-freeze (agent)

1. Rebuild, producing a new `clean/MANIFEST.json` hash.
2. **Re-run the hash-bound evidence** — keyboard traversal at literal 390 across six pages,
   200% zoom pre-screen. Both died with the old hash.
3. Re-freeze and record the new hashes everywhere they are cited.

**Method note:** use CDP with `Emulation.setDeviceMetricsOverride` and `Page.bringToFront`.
Do **not** activate Chrome by app name — two instances share a bundle id on this machine and it
silently targets the wrong window. That caused every earlier traversal failure.

**Any capture must scroll top→bottom and wait on `document.images` before shooting.** Skipping
this is what produced the entire false `IMG-001`–`IMG-004` finding set.

---

## PHASE 4 — The gates (Nate, plus bounded agent work)

`CR-0` review PASS on the new artifact → `CR-1` audience/channel/wording decisions →
`CR-2` review hub build → `CR-3` metadata → `CR-4` the send.

`CR-3` has real work: **zero OG tags and no canonical on all six pages** (verified). Owned by
`LINK-PREVIEW-METADATA-AUDIT-20260802.md`.

**`noindex,nofollow` is correct** for a private review package and must NOT be removed. Three
separate audits recommend removing it; all three are wrong for this artifact.

---

## FOUR THINGS THAT WILL BITE IF NOT HANDLED FIRST

**1. The deterministic checker is failing — 26 errors.** `scripts/check-maplemoon-review.py`
carries ~157 uncommitted lines pinned to `SAT-HOME-CLEAN-CLOSURE-01`, a packet that is not
happening. A failing QA gate cannot be carried into a client send. **Codex decision, still open
since 2026-08-02** — revert the checker or re-pin it. Blocks Phase 3.

**2. `staging-v1/` is gitignored with zero tracked files.** The artifact this project exists to
send has **no committed copy**; its only integrity evidence is a SHA match. **A rebuild is the
single riskiest moment for it.** Take a checkpoint before Phase 2 mutates anything. Changing
`.gitignore` is Nate's and Codex's call, not a worker's.

**3. Codex's visual QA findings are artifact-contaminated.** `visual/findings.json` and the
review board built over those captures carry `VIS-001` and `IMG-001`–`IMG-004`, all disproved
live. **Twelve QA packets are currently in flight over that data.** The captures need retaking
with a scroll-and-settle pass before any of it drives a fix batch. Flagged, not touched —
Codex's lane.

**4. There is still no send date.** A project with no committed next date drifts by default,
which is exactly the pattern that produced the missed 1 August. **Set one and work backwards.**

---

## The honest summary

The package is in **far better shape than three consecutive reports claimed** — most of the
alarming findings were capture artifacts. What genuinely remains is four small defects, a
naming change now decided, and an icon set that can follow later.

**The project is not blocked on building. It is blocked on one person finishing a 30-minute
review pass**, and it has been for two days.

## Lane compliance

Written inside `_wip/evidence/` only. Nothing built, fixed, rebuilt, recorded or sent. No gate
written. `NATE-HOME-001` untouched at `pending`.
