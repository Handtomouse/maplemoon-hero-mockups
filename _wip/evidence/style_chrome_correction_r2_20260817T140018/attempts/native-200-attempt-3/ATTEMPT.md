# Native-200 attempt 3

Outcome: **HARNESS HOLD — first control socket timed out.**

All three profiles launched, but the first profile's CDP connection was left
idle while the later profiles initialised and timed out on the redundant
homepage navigation used by the accepted harness. The R2 wrapper now records
each native preference control immediately after its isolated launch. It still
uses the pinned accepted metrics/calibration primitives, native profile zoom,
and identical control thresholds; it does not use emulation or input methods.
