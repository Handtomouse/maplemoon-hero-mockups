# GO: macbook → iMac worker
# Type: GO (nudge — the REQUEST is already in your mailbox and unchanged)
# Task: MAPLEMOON-KEYBOARD-PROOF-20260802 (homepage addendum)
# Written: 2026-08-02 23:30 AEST
# Authorised by: Nate, directly.

## Start immediately. Preconditions are already verified.

The original brief is `REQUEST-HOMEPAGE-390-20260802-114222.md`, same directory, unchanged.
Nothing in it has been superseded. This note exists only to say the way is clear.

## Freeze window held — verified from the macbook at 23:30 AEST

Your copy of the artifact is byte-identical to ours. Checked just now:

| File | macbook | iMac | |
|---|---|---|---|
| `staging-v1/clean/MANIFEST.json` | `d1c66b1d5937e995…` | `d1c66b1d5937e995…` | MATCH |
| `staging-v1/annotated/MANIFEST.json` | `3be3c0f2df465855…` | `3be3c0f2df465855…` | MATCH |
| `staging-v1/clean/homepage.html` | `0d102050395b79f4…` | `0d102050395b79f4…` | MATCH |

`_wip/evidence/IMAC-KEYBOARD-PROOF-20260802/HARNESS.py` is present on your side.
`RESULTS.json` on your side still has no `homepage` entry, so this run is genuinely outstanding.

## Note on your clone being behind

Your `HEAD` is `d65047b`; the macbook is now at `46410be`. **Do not pull to fix this.**

The commits between are evidence, orchestration and imagery-custody records that have no
bearing on this run, and `staging-v1/` is gitignored — it never travelled by git in the first
place. The artifact you hold was rsynced and is hash-verified above, which is the only thing
that matters here. Pulling risks disturbing a proven-good working copy for no gain.

## The ask, restated in one line

**`homepage.html` — real OS-level keyboard traversal at literal 390 CSS px, identical method
to the five you already completed.** Do not improve the method; homogeneity across all six
rows is the entire value of the record.

## Return

Append homepage to `RESULTS.json` in the same shape as the other five, keep the raw per-page
JSON and screenshot alongside them, and drop a COMPLETION note in
`_wip/evidence/HANDOFFS/IMAC-TO-MACBOOK/`.

If anything blocks you, write a BLOCKED note in that same directory rather than improvising a
workaround or a different vehicle.
