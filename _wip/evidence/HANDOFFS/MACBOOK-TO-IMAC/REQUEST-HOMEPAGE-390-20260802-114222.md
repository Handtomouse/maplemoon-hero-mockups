# REQUEST: macbook Claude → iMac worker
# Type: REQUEST
# Task: MAPLEMOON-KEYBOARD-PROOF-20260802 (addendum)
# Written: 2026-08-02T11:42:22Z
# Authorised by: Nate, directly.

## One page. One run. Close the last evidence gap.

**`homepage.html` — real OS-level keyboard traversal at literal 390 CSS px.**

That is the entire request. Everything else about the six-page package is now covered.

## Why this specific run

Your last run produced our-story, carob-story, stockists, faq and shop at literal 390 CSS
px. Homepage has keyboard evidence **only at 500 CSS px**, from an earlier session using the
browser-extension vehicle.

So five rows of the record match each other and one does not — different viewport, different
input vehicle. The value of that record is that every row was produced the same way. One
odd row out undermines it.

## Method — identical to your last run, deliberately

Do **not** improve it. Use exactly what produced the other five:

- isolated Chrome, `--user-data-dir=/tmp/mm-chrome-profile`, `--remote-debugging-port`
- **CDP `Page.bringToFront`** to target the specific window — never `activate` by app name.
  That ambiguity, with two Chrome instances sharing a bundle id, was the root cause of every
  failed traversal including mine.
- `osascript` for the raw Tab keystroke only
- `Emulation.setDeviceMetricsOverride` → literal 390×667 CSS px
- assert `document.hasFocus()` true at start AND end; abort as UNVERIFIED if focus is lost
- retain the FULL ordered stop list — this is non-negotiable, it is why the shop gap was
  solvable at all
- record `wrapped_at`, `zero_size_focus_stops`, `focus_leak_into_closed_cart_dialog`,
  `closed_dialog_inert`, `closed_dialog_aria_hidden`
- `screenshot_homepage_t0.png` as frontmost/viewport proof
- keep the BODY pseudo-stop convention (`body: true`) so counts stay comparable

If focus will not advance, STOP and report. Do not substitute a weaker input method — a
contaminated row is worse than a missing one.

## Expected result, for your own sanity check

Static DOM baseline at 500 CSS px was **39 tabbable / 33 unique**, first stop
"Skip to main content".

At 390 the mobile breakpoint engages, so expect a small delta — the other five pages came in
within ±1 of their 500px baselines once the BODY pseudo-stop was excluded. Cart dialog is
`#mmCartDialog`; expect closed, `inert`, `aria-hidden="true"`, **zero** leaks.

**Apply the `COVERAGE_SUSPECT` guard:** if unique controls come back under half the static
tabbable count, flag it even if the traversal technically completed. That is exactly the
signature shop showed, and it hid for a day because only aggregates were retained.

## Artifact — verify before running, do not skip

    clean/MANIFEST.json     d1c66b1d5937e9956d1806dbbbff4faf4f950def046aa642ae52f809c3d52d20
    annotated/MANIFEST.json 3be3c0f2df4658558c667b3e9cc6d55966d6a1d7ce9fa9874c46a0afc44244c7

Unchanged since your last run. A review of a drifted artifact is void — if these do not
match, STOP and report.

**Serve loopback only:** `python3 -m http.server 3011 --bind 127.0.0.1` from
`docs/client-review/2026-08-01-saturday-review/staging-v1/clean/`.

**Hard load guard:** confirm the page actually loaded before auditing. A run on this side was
discarded for auditing Chrome's *error page* after the server died — it reported implausibly
few controls, which is what gave it away. Assert a known homepage control exists before
counting anything.

## Output

    _wip/evidence/IMAC-KEYBOARD-PROOF-20260802-HOMEPAGE/RESULTS.json

A **separate** directory. Do not modify the existing `IMAC-KEYBOARD-PROOF-20260802/`
RESULTS.json — it is committed at `9ead886` and I verify its hash. I will merge the records
on this side.

## Hand-back

Leave it in your clone. I collect directly over the SMB mount at
`/Volumes/handtomouse/maplemoon-website/` — no scp needed, and scp to the macbook still
fails because Remote Login is off here.

## Authority

Evidence only. No verdict. Nothing promoted. CR-0 through CR-4 remain Nate's alone — he is
doing his CR-0 pass now, in parallel with this run.

Do not touch `staging-v1/`, `docs/orchestration/`, `LOCK_MANIFEST.json`,
`scripts/check-maplemoon-review.py`, or `.gitignore`.

— macbook Claude (peer worker, not custodian)
